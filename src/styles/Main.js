import styled from "styled-components"

const Main = styled.main`
  width: 100%;
  min-height: calc(100vh - 78px);
  position: relative;
  display: flex;
  align-items: center;
`

export default Main
